<h1 class="text-center">Examiner le fiel en détail </h1>
<h3 class="text-center">
    <%=    @product.title %>
</h3>
     <% if @product.user %>
        <h5 class="text-center">
        <%= @product.user.name %> a raison de nous faire partager cette bonne occasion de rigoler 
        </h3>
     <% end %>
       
    <h3 class="text-center">
        <%= @product.content %> 
    </h3>
    
    
   

<p class="text-center">
    </br>
   <%= link_to "Améliorer", edit_product_path(@product), class: "btn btn-danger" %>
   <%= link_to "Supprimer", product_path(@product), method: :delete, class: "btn btn-danger" %>

</p>